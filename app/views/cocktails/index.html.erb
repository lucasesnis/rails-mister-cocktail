<h1> All Cocktails </h1>

<div>
  <% @cocktails.each do |cocktail| %>
    <p><%= cocktail.name %></p>
    <%= cl_image_tag cocktail.photo.key, height: 300, width: 400, crop: :fill %>
  <% end %>
</div>
